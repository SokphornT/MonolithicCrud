spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/Sokphorn_CRUD
    username: postgres
    password: st1998@1998@
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health

  endpoint:
    health:
      show-details: always